# Prompt_Engineering
For developing tools that integrate LLMs with my data.

## MySQL News MCP

A remote MCP server for ChatGPT agents/apps mode is included under `mcp_news_server/`.

- Deployment and operations guide: `docs/MCP_DEPLOYMENT.md`
- Daily DB sync scripts: `scripts/export_news_dump.ps1` and `scripts/import_news_dump.sh`
